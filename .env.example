# Host port used by the Unity ROS-TCP-Connector.
UNITY_TCP_PORT=10000

# cuda or cpu. The CPU Compose override sets both to cpu automatically.
YOLO_DEVICE=cuda
NMPC_DEVICE=cuda

# Required when wandb_mode is "online".
WANDB_API_KEY=

DISPLAY=
